# Report 1 Evidence Packets

These folders were formerly top-level `experiments/report1_*` directories.
They are grouped here to keep `experiments/` navigable.

Use the summaries and small source tables as provenance. Treat large grids as
transient unless a summary explicitly names them as promoted reference data.
