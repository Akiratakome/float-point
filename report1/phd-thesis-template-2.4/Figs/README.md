# Figs Directory

This directory is used by the CUED class/title page for crest files. Keep the
existing crest paths stable.

Report evidence figures should normally stay in `experiments/` and be referenced
from the manuscript only after final figure selection. Do not copy raw grids or
large transient outputs into this directory.
