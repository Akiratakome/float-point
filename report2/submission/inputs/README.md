# PDF inputs

Place or generate:

- `report1.pdf` -- final Report 1, normally the already submitted version unless
  a deliberate, logged correction has been made;
- `report2.pdf` -- final standalone Report 2 used for Overleaf word counting.

The PDFs are ignored by Git. `prepare_and_build.ps1` populates them from the
local report workspaces.
