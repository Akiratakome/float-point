# Chapter 4 未完成实验与内容复核

日期：2026-07-29

## 1. 结论

原先缺失的 Orszag--Tang / HLLD / fp64 / $512^2$ 端点已经补全，不再作为论文负结果。
最终实现对二阶更新先形成候选状态；如果某一守恒更新线出现非物理候选，则整条线使用共享的
一阶 HLL 界面通量重新更新。该处理保持界面通量一致，不钳制压力，也不改变 cfg 默认值。

同一最终 O2 二进制在 CFL 0.2 下完成 $128^2$、$256^2$ 和 $512^2$ 三个端点至
$t=0.5$。$512^2$ 运行返回码为 0，完成 3277 步；独立读取保存网格得到
$\rho_{\min}=1.0976039403$、$p_{\min}=0.3798552929$，全部变量有限。

因此 Chapter 4 使用 24/24 完成、8/8 完整三网格组的口径。旧负压退出记录只保留为
内部缺陷定位 provenance，不进入论文正文、图注或结论。

## 2. 修复与验证

- 根因：MUSCL--Hancock/HLLD 二阶单元更新没有正性保持或一阶通量修正；原代码只在 x/y
  两次扫掠之后检查物理状态，发现问题时只能退出。
- 修复：若一条 x 行或 y 列的二阶候选出现 $\rho\le0$、$p\le0$ 或非有限值，则使用
  一阶 HLL 通量守恒地重算整条线。
- 单元回归：双精度完整测试通过，198 个测试、27081 个断言。
- 三网格完成：步数分别为 801、1622、3277；三个保存网格均为 fp64、有限且正压。
- 自加密诊断：密度 mean-$L_1$ 差异为 0.07514601996 和 0.04181536595，observed
  $p=0.8456635061$。该值只说明所测三网格的自一致趋势，不证明渐近收敛。
- 权威记录：`experiments/diagnostics/hlld_positivity_guard/summary.json`；最终二进制
  SHA-256 为 `6cb38f91936bfa749a61fd37cfe0986d14aeec1c45764442071ea9092f0d3ccd`。

## 3. Chapter 4 写作决定

- Figure 4.2(a) 显示八个完整三网格组，并且只绘制通过物理状态检查的有效数值。
- OT 小节报告 HLLD fp64 $512^2$ 的完成步数和有限正压检查，并报告该组三网格
  observed $p\simeq0.846$。
- Validation limits 保留“无二维精确解”“三个网格不证明渐近区间”“不同 CFL 不支持
  HLL/HLLD 排名”和“GPU 只覆盖 HLL Brio--Wu/OT”等边界。
- 不把修复运行耗时写入性能比较；该构建用于数值完成和物理状态验收。

## 4. 仍未补全但不阻塞 Chapter 4 的项目

| 项目 | 当前事实 | 处理决定 |
|---|---|---|
| 完整 KH MCA | 本地全尺寸运行超出预算；已有缩小 smoke | 属于 Chapter 5 精度证据，不能写成已完成 |
| HLLD-on-GPU、KH-on-GPU、GPU MCA | CUDA MHD 路径只验证 HLL Brio--Wu/OT | 不扩写现有 GPU 结论 |
| Lecoanet 非线性 $Re=10^5$ 参考 | 缺少显式黏性、热扩散和被动染料 | 只保留初值与早期增长复现 |
| 二维精确解或渐近收敛 | OT/KH 为含激波和细电流片的自加密诊断 | 不改写为 accuracy 或 asymptotic convergence |

## 5. Review 结论

Chapter 4 的数值缺口已经关闭，计划与正文应统一使用成功结果。语言仍保持 Report 1 的
结果写法：先点名算例或图表，再给数字、解释含义并立即限定范围。Chapter 4 状态继续为
`author-rewrite`，直到学生改写、Overleaf 正式词数和最终人工事实核对完成。
